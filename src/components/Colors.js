import React from "react";
import { Link } from "react-router-dom";
import { NavLink } from "react-router-dom";
import styled from "styled-components";

const Colors = {
  activeColor: "hsl(333deg, 100%, 50%)",
};

export default Colors;
